<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main">
    
    <!-- 数字人渲染视图（全屏背景） -->
    <im.zego.digitalmobile.ZegoDigitalView
        android:id="@+id/digital_human_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    
    <!-- 占位视图（覆盖在数字人上） -->
    <com.example.zegodigitalhumanquickstart.view.ZegoQuickStartDigitalHumanPlaceholderView
        android:id="@+id/placeholder_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible" />
    
    <!-- 状态标签（顶部悬浮） -->
    <TextView
        android:id="@+id/status_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="56dp"
        android:gravity="center"
        android:text="@string/status_idle"
        android:textColor="@color/text_white"
        android:textSize="14sp"
        android:layout_gravity="top|center_horizontal" />
    
    <!-- 控制面板切换按钮（放在控制面板层级下面，控制面板隐藏时显示） -->
    <Button
        android:id="@+id/toggle_control_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:backgroundTint="#E6FFFFFF"
        android:text="@string/toggle_panel_open"
        android:textColor="@color/primary_color"
        android:textSize="16sp"
        android:elevation="8dp"
        android:layout_gravity="bottom|center_horizontal" />
    
    <!-- 控制面板容器（底部弹出，覆盖在按钮上方） -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/control_panel_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="450dp"
        android:background="#F2FFFFFF"
        android:visibility="invisible"
        android:layout_gravity="bottom"
        android:fillViewport="false"
        android:elevation="16dp"
        app:behavior_hideable="true"
        app:behavior_peekHeight="0dp"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">
        
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="0dp">
            
            <!-- 任务控制视图 -->
            <com.example.zegodigitalhumanquickstart.view.ZegoQuickStartTaskControlView
                android:id="@+id/task_control_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp" />
            
            <!-- 驱动控制视图 -->
            <com.example.zegodigitalhumanquickstart.view.ZegoQuickStartDriveControlView
                android:id="@+id/drive_control_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="15dp"
                android:layout_marginBottom="20dp" />
            
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
